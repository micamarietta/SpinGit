using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UISelectSpinner : MonoBehaviour
{
    /*
    public Canvas canvas;
    public Button spinnerIcon;

    public void SelectSpinnerButton()
    {
        canvas = canvas.GetComponent<Canvas>();
        spinnerIcon = spinnerIcon.GetComponent<Button>();

        // [SPRINT 4] TODO 1:
        // int selectedPosX = spinnerIcon.PositionX;
        // int selectedPosY = spinnerIcon.PositionY;

        ProfileManager.Instance.SelectSpinnerPM(selectedPosX, selectedPosY);
    }
    */
}
